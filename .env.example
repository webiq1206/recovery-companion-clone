# Copy to `.env` for local native builds (never commit real keys).
# EAS: create project secrets with the same names so `eas build` embeds them.
EXPO_PUBLIC_REVENUECAT_IOS_API_KEY=
EXPO_PUBLIC_REVENUECAT_ANDROID_API_KEY=
# Optional: only if your RevenueCat entitlement id differs from the default "RecoveryRoad Pro"
# EXPO_PUBLIC_REVENUECAT_ENTITLEMENT_ID=
