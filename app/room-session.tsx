import { Redirect } from 'expo-router';

/** Archived implementation: `archived/community_features/app/room-session.tsx` */
export default function RoomSessionArchivedPlaceholder() {
  return <Redirect href="/(tabs)/connection" />;
}
