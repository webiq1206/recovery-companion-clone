import { Redirect } from 'expo-router';

/** Archived implementation: `archived/community_features/app/recovery-rooms.tsx` */
export default function RecoveryRoomsArchivedPlaceholder() {
  return <Redirect href="/(tabs)/connection" />;
}
