import { Redirect } from 'expo-router';

/** Archived implementation: `archived/community_features/app/tabs_community/` */
export default function CommunityTabArchivedPlaceholder() {
  return <Redirect href="/(tabs)/connection" />;
}
