import { Redirect } from 'expo-router';

/** Archived: `archived/community_features/app/tabs_connection/chat-paths.tsx` */
export default function ChatPathsArchivedPlaceholder() {
  return <Redirect href="/(tabs)/connection" />;
}
