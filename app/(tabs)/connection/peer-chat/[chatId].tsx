import { Redirect } from 'expo-router';

/** Archived: `archived/community_features/app/tabs_connection/peer-chat/[chatId].tsx` */
export default function PeerChatArchivedPlaceholder() {
  return <Redirect href="/(tabs)/connection" />;
}
