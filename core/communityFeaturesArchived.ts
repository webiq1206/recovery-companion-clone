/**
 * Master switch for social / peer-practice surfaces archived under `archived/community_features/`.
 * When false: providers are stubbed, routes redirect, and copy paths avoid social/community UI.
 * Restore: set to `true`, move archived implementations back into `providers/`, `services/`, `app/`, etc.
 */
export const isCommunityFeatureEnabled = false;
