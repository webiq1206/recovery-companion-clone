/**
 * Settings UI toggles. Flip flags to re-enable hidden sections without restoring code from git.
 */

/** Account heading and Delete account block on Settings. */
export const SHOW_SETTINGS_ACCOUNT_SECTION = false;
